# Changelog

## 0.1.80 - 2026-08-13

+ [修复] 上游在生成中返回未闭合的图片参数 JSON 片段时，识别为未完成的参数回显并自动切换账号重试，不再将截断的 `size`、`n`、`prompt` 内容作为 `image_response_text` 返回。
+ [测试] 增加截断参数对象及普通文本防误判回归测试。

## 0.1.79 - 2026-08-13

+ [修复] 上游返回完整图片参数对象但 `prompt:null`、且没有图片 ID 时，识别为未启动生图并自动切换账号重试；避免将内部参数 JSON 返回给用户。
+ [修复] 异步生图任务遇到免费额度耗尽账号时立即移除并切换下一账号，额度账号不消耗任务正常重试次数；号池全部耗尽时统一返回 `image_quota_exhausted`。
+ [调整] 后台与外置账户导入接口均在账号落库后立即返回，并自动启动轻量额度与凭证验证；验证完成前账号不参与调度，导入请求不等待验证结果。

## 0.1.78 - 2026-08-13

+ [修复] 上游只回显标准图片请求参数、未触发生图工具时，自动冷却当前账号并切换账号重试；参考图文件 ID 回显继续适用同一流程。
+ [调整] 上述回显在所有重试耗尽后不再返回内部请求 JSON，统一返回内容策略提示和 `content_policy_violation` 错误码。

## 0.1.77 - 2026-08-13

+ [修复] Dashboard 最近失败列表保留接口实际返回的错误标题、原始文本和建议，不再把上游文本或内容拦截二次归类为通用请求参数错误。
+ [调整] `/health/stability` 将调用统计由最近 60 条改为最近 1 小时全部调用，字段更名为 `recent_hour` 并返回统计时间窗。

## 0.1.76 - 2026-08-13

+ [修复] 图片编辑上游仅回显 `referenced_image_ids` 和请求参数、未返回图片引用时，视为当前账号未启动生图，自动切换下一账号重试；不再将该参数回显返回给调用方。

## 0.1.75 - 2026-08-13

+ [修复] 将上游 `You've hit the Free plan limit for image generations requests` 识别为图片额度耗尽，立即移除当前账号并切换下一账号重试；全部耗尽时返回“号池额度不足”。
+ [调整] 账号导入携带 `refresh:true` 时改为异步校验可用性与图片额度，接口立即返回 `refresh_id`，待验证账号在检查成功前不会参与调度。
+ [修复] 管理端任务的 `image_response_text` 直接展示上游返回的 `error.message`，保留内部错误码，不再误报为请求参数错误。

## Unreleased

## 0.1.74 - 2026-08-12

+ [修复] 参考图已上传但上游返回 `referenced_image_ids` 且 `prompt: null` 时，记录完整绑定诊断并冷却当前账号、切换账号重试。
+ [新增] 参考图上传完成、会话绑定和上游未启动生图的诊断日志，使用脱敏文件标识关联同一任务。
+ [调整] `/v1/images/edits` 应用层请求体上限由 64 MiB 提升至 128 MiB，与 Nginx 配置保持一致。

## 0.1.73 - 2026-08-12

+ [修复] 恢复 Dashboard 并发组件共用的数值边界函数，修复首页加载时 `clamp is not defined` 的客户端异常。
+ [新增] 私有仓库部署支持 `IMAGE_POOL_GITHUB_TOKEN`：后端查询 GitHub Release、Tag 和 Changelog 时自动鉴权，Token 不会发送到 CDN 或返回给前端。
+ [调整] 更新器读取宿主机 GHCR 登录凭据，可拉取私有 `ghcr.io` 镜像；固定镜像标签时仍需改为 `latest` 才能使用在线更新。

## 0.1.72 - 2026-08-12

+ [调整] 移除后台调用趋势图及其历史统计聚合，Dashboard 保留今日调用、并发、资源和异常状态。
+ [调整] 移除 SCUNet 高清修复功能、设置入口、请求参数、任务状态、诊断数据及 Docker 运行依赖。
+ [优化] 图片生成或编辑继续保留参考图、缓存下载和输出格式转换，不再经过本地图像后处理。
+ [修复] 上游未返回图片但返回 assistant 文本时直接返回可读文本；`skipped_mainline` 纳入切号重试。

## 0.1.71 - 2026-08-12

+ [修复] 轮询到 assistant 文本而未出现图片工具或图片引用时立即结束请求，不再等待“图片生成长时间无结果”。
+ [调整] 图片参数确认文本统一返回“OAI侧未识别到提交生图参数，请重新提交”；普通文本直接返回，内容拦截返回固定的提示词修改说明。
+ [调整] 上述请求或内容结果不再切换账号，也不计入账号异常。

## 0.1.70 - 2026-08-10

+ [修复] 检测到 403 时自动合并触发 FlareSolverr，成功后仅重试一次并复用新的 Clearance，避免并发请求重复触发。
+ [调整] 默认 FlareSolverr 地址改为 Docker Compose 服务地址 `http://flaresolverr:8191`，新部署无需手动填写。
+ [测试] 增加 FlareSolverr 自动挑战处理和默认地址回归测试。

## 0.1.69 - 2026-08-10

+ [修复] 统一图片接口的参数、参考图、轮询、账号和服务异常返回，提供稳定的 HTTP 状态码与 `error.code`。
+ [修复] 清理错误响应中的服务地址、内部路径和凭证诊断信息，同时保留可读的实际请求原因与处理建议。
+ [新增] 参考图请求体超过 64 MiB 时返回 HTTP 413 与 `request_body_too_large`。

## 0.1.68 - 2026-08-08

+ [新增] 后台支持切换单号并发槽位模式：动态模式按账号健康度自动升降，静态模式直接使用配置的单号最大并发。
+ [调整] 动态/静态模式切换后立即应用到账号租约调度，账号失效、限流和额度耗尽处理保持不变。
+ [测试] 增加静态单号并发上限、失败后保持槽位及配置持久化回归测试。

## 0.1.67 - 2026-08-08

+ [修复] 上游要求上传缩略图或参考图时直接返回请求错误，释放账号租约并结束当前任务。
+ [修复] 缺少参考图和内容安全结果不计入账户生图异常数，真实上游生图异常继续按原策略记录。
+ [测试] 增加参考图请求错误分类、租约释放和账户异常计数回归测试。

## 0.1.66 - 2026-08-08

+ [新增] 异常或切号结束时记录上游 assistant 文本的限长前缀和字符数，便于确认无图片节点时上游实际返回内容。
+ [优化] 轮询心跳继续使用脱敏结构摘要，避免重复写入文本；正常包含图片工具的响应不记录 assistant 文本。
+ [测试] 增加 assistant 文本提取、限长和终止诊断日志回归测试。

## 0.1.65 - 2026-08-06

+ [修复] 公开返图 `/images/` 与缩略图 `/image-thumbnails/` 补充跨域响应头，外部站点可直接读取图片 URL。
+ [修复] 公开返图支持 `OPTIONS` 预检与 `HEAD` 请求，并公开 Range、长度和缓存相关响应头。
+ [测试] 增加跨域图片读取与预检请求回归测试。

## 0.1.64 - 2026-08-06

+ [新增] 增加生图上游 SSE、会话轮询和图片引用的脱敏诊断日志，记录响应结构、引用计数、状态标记和短哈希。
+ [优化] 生图切号与完成日志关联上游响应诊断，便于区分上游未返回图片引用和本地提取异常。
+ [测试] 增加嵌套图片引用、排除上传参考图和敏感值脱敏的回归覆盖。

## 0.1.63 - 2026-08-06

+ [调整] 账号导入默认在持久化完成后立即返回，注册机批量上传不再同步等待上游可用性检查。
+ [调整] `/api/accounts` 与 `/api/external/accounts/import` 支持 `refresh:true` 显式执行同步首次检查，后台管理页面保留原有验证体验。
+ [测试] 增加默认快速入池、显式刷新和外部导入接口的回归覆盖。

## 0.1.61 - 2026-08-05

+ [新增] 为每个账号增加独立生图租约和动态槽位控制，单号并发按账号分别调度。
+ [优化] `polling_image` 记录会话、HTTP 状态、工具节点、图片引用、额度和切号上下文，账号失效或生图长尾时自动切换。
+ [新增] 管理员任务详情展示账号尝试、租约、耗时和失败原因，普通用户响应继续脱敏。
+ [优化] 设置页收拢高级调度参数，保留全局并发、单号并发和 SCUNet 高清修复入口。

## 0.1.60 - 2026-08-05

+ [新增] 容量接口增加单号并发参与的即时并发槽位、单号预计可生成量和号池预计可生成总量。
+ [优化] 容量预测按当前可调度账号计算，额度已耗尽或处于冷却状态的账号不计入即时容量。
+ [新增] 后台总览展示单号并发、当前并发槽、单号余量、号池余量和额度耗尽账号数量。
+ [测试] 增加额度耗尽账号、单号并发和动态补号容量预测的回归覆盖。

## 0.1.59 - 2026-08-05

+ [修复] 统一后台容量预测与自动补号控制器的目标口径，修复任务压力下页面显示“建议补 0”但控制器已经启动补号的问题。
+ [新增] 容量接口和自动补号状态返回所需有效账号、建议补充账号及注册尝试数量，注册机可直接读取统一后的 `estimate.recommended_register_accounts`。
+ [测试] 增加 17 个有效账号面对 35 个任务压力时建议补充 18 个账号的回归测试。

## 0.1.58 - 2026-08-05

+ [新增] 号池容量改为按当前排队/运行任务预测所需有效账号；空闲时不再因突发并发基线建议补号。
+ [新增] 增加按任务压力自动补号控制器：支持活跃最低账号、空闲保底、有效账号上限、批次大小、批次冷却和静默时间；连续空闲后停止注册，正在注册中的账号计入批次目标。
+ [优化] 注册管理器在“按有效账号数”模式下预留已启动 worker，避免高线程配置超过目标账号数。

## 0.1.57 - 2026-08-04

+ [修复] 上游任意图片链路返回 HTTP 401 时立即移除失效账号，释放当前资源并切换号池账号重试；覆盖生图结果下载与 Base64 图片下载。

## 0.1.56 - 2026-08-04

+ [新增] 增加号池全局、准备、提交、轮询、下载和参考图上传分层并发配置，并在后台总览显示实时占用与等待量。
+ [优化] 任务等待账号或全局并发槽位时释放本地执行槽，资源可用后再重新进入执行阶段，避免账号池繁忙时 worker 被长期占用。
+ [新增] 生图卡住诊断与切号策略：连续无图片引用达到阈值后冷却当前账号、创建新会话重试，并记录轮询次数、HTTP 状态、账号额度和累计切号次数。

## 0.1.55 - 2026-08-03

+ [调整] 图片轮询上限从 300 秒提升至 600 秒（10 分钟），生图总预算同步调整为 630 秒。

## 0.1.54 - 2026-08-03

+ [调整] 参考图上传上限统一提升至 10 张，保留单张 20MB 和总大小 75MB 限制。

## 0.1.53 - 2026-08-02

+ [修复] 任务实时状态弹窗首次加载时任务对象为空导致前端读取 `used_accounts` 崩溃。

## 0.1.52 - 2026-08-02

+ [新增] 后台任务队列展示当前使用账号及任务完成后的账号使用汇总，包括邮箱/账号 ID、可用图片额度、状态、尝试次数和自动移除标记。
+ [调整] 管理员任务接口保留账号使用信息，普通用户接口继续隐藏账号标识和内部调度统计。

## 0.1.51 - 2026-08-02

+ [调整] 移除后台“修复记录”列表及其历史接口，保留 SCUNet 高清修复处理队列和配置入口。
+ [修复] 上游确认图片额度耗尽时自动移除对应账号并记录删除原因；普通临时 429 继续使用冷却策略。

## 0.1.50 - 2026-07-31

+ [新增] 新增 `image_account_max_inflight_per_account` 配置，可控制单个账号同时承载的生图任务数，默认 1、最高 20。
+ [调整] 移除本地图像放大处理、2K 模型别名和相关配置入口，图片后处理保留 SCUNet 高清修复。

## 0.1.45 - 2026-07-28

+ [优化] 图片管理改为服务端分页、搜索和筛选，仅为当前页读取图片尺寸；合并图片列表、标签与存储统计请求，移除分页响应中的重复分组数据，降低加载耗时和传输体积。

## 0.1.44 - 2026-07-28

+ [优化] 为任务时间、状态和所属用户查询增加数据库索引，精简任务历史、控制台及状态轮询响应，减少全表扫描和 Base64 图片数据传输，提升后台页面加载速度。

## 0.1.43 - 2026-07-28

+ [修复] Dashboard 任务队列改为统计真实的排队、处理中和活动任务数量，不再仅以瞬时调度通道长度显示为零；保留通道容量并同步修正进度展示。

## 0.1.42 - 2026-07-28

+ [新增] 生图请求支持异步任务队列、状态查询和回调通知，账号测试生图可选择模型和自定义提示词，生成结果统一进入图片管理。
+ [新增] 图片后处理队列与任务列表，支持 SCUNet 高清修复、处理前后对比；SCUNet 使用分块推理降低内存占用。
+ [修复] 图片自动清理按保留天数生效，并支持立即释放昨日及更早的图片，同时展示预计清理数量。
+ [优化] 后处理默认关闭；重整业务调度、调用趋势、错误率及任务列表布局，并改进曲线显示和面板间距。
+ [调整] 移除 Bark 推送、控制台日志级别和“清理到目标剩余空间”等设置，开关切换后立即保存生效。

## 0.1.41 - 2026-07-27

+ [新增] ChatGPT Web 上游未提供 `usage` 时，按提示词、参考图、输出尺寸、质量和生成数量估算并返回 `input_tokens`、`output_tokens` 与 `total_tokens`，供 OpenAI 兼容渠道统计图片生成吞吐量。

## 0.1.40 - 2026-07-22

+ [新增] 生图 API、任务历史和调用指标统一输出稳定错误码、中文标题、错误分类、重试建议及请求追踪 ID。
+ [优化] Dashboard 按稳定错误码聚合失败原因，并在人性化错误列表和任务详情中展示分类与处理建议。
+ [隐私] 公开图片与任务响应隐藏账号邮箱、账号使用数量、切号次数和内部重试明细，服务端日志保留排障信息。
+ [兼容] 旧版动态超时、认证恢复及“任务占用额度失败”记录会自动归并到对应错误码，无需迁移数据库。

## 0.1.39 - 2026-07-22

+ [稳定性] 参考图上传使用独立 60 秒预算和进程级并发控制，并对文件创建、Blob PUT、确认阶段的瞬时网络错误、限流及 5xx 响应进行有界重试。
+ [修复] 对齐当前 ChatGPT Web 文件上传字段；已接受的 SSE 生图会话不再被提前关闭，恢复请求遇到短暂 429 时保持原账号和会话继续重试。
+ [优化] 后台账号刷新跳过正在生图的租约账号，限制刷新并发与单账号验证时长，避免刷新任务争抢生图连接和账号。
+ [调整] 任务列表、历史记录、调用统计及图片响应统一显示公开模型名 `gpt-image-2`，实际上游模型 slug 仍由账号和路由内部选择。

## 0.1.38 - 2026-07-21

+ [修复] 识别新版 ImageGen 工具通过 `metadata.is_error=true` 和错误文本返回的终态，立即结束当前会话并切换账号重试。
+ [修复] 任务达到 300 秒截止时记录为 OAI 生图超时，仅主动取消或服务关闭显示“任务已取消”。

## 0.1.37 - 2026-07-21

+ [修复] 将公开的 `gpt-image-2` 请求映射到当前可用的 `codex-gpt-image-2` Web 图片路由，避免 `auto` 被上游解析为普通文本模型。
+ [修复] 收到可恢复会话 token 后快速切换到恢复流，减少无效 SSE 等待。

## 0.1.36 - 2026-07-21

+ [修复] 已创建 `conversation_id` 的生图任务持续等待同一上游会话，不再因“图片仍在生成”状态切换账号；已提交会话真实等待上限调整为 300 秒。
+ [调整] OAI 侧生图超过 300 秒时仍返回 HTTP 429，但对外文案改为“OAI侧出图超出300s，请重新提交任务。”并标记为超时错误。
+ [修复] 设置页“图片自动清理”新增后端持久化字段，保存 7 天等自定义值后不再回退到 30 天。
+ [优化] 设置页生图轮询/总预算展示同步为 300/330 秒，并更新示例配置。

## 0.1.35 - 2026-07-20

+ [修复] 对齐官方 ChatGPT Web：`/f/conversation/prepare` 返回 `status=ok` 但 `conduit_token` 为空时不再判定为账号凭证失效，而是继续提交 `/f/conversation` 并省略 `X-Conduit-Token`。
+ [修复] 空 `conduit_token` 不再归类为 `token_revoked`/认证失败，避免把全量可用账号误删或反复切号。
+ [优化] `status=ok` 的空 conduit prepare 不再刷错误日志，仅对异常 prepare payload 保留诊断日志。

## 0.1.34 - 2026-07-20

+ [修复] ChatGPT Web 协议头改为从首页实时 `data-build/data-seq` 同步 `OAI-Client-Version` 与 `OAI-Client-Build-Number`，避免长期使用过期客户端标识。
+ [修复] 生图 prepare 请求对齐官方当前包体，补充 `messages` 与 prepare dispatch/source 字段，并取消初始请求中的伪 `X-Conduit-Token: no-token`。
+ [修复] 对 `status=ok` 但 `conduit_token` 持续为空的账号按半失效凭证处理，自动移除并切换其他账号，避免半失效账号反复进入调度导致高错误率。

## 0.1.33 - 2026-07-20

+ [修复] ChatGPT Web 生图 prepare 在 `status=ok` 但 `conduit_token` 为空时，会在同账号短间隔重试，减少高并发下误切号和任务失败。
+ [诊断] prepare 缺少 `conduit_token` 的日志补充 `status` 字段，便于判断上游空 token 是否为临时态。

## 0.1.32 - 2026-07-20

+ [修复] ChatGPT Web 生图 `conversation/prepare` 请求补入当前协议需要的 `partial_query`，避免上游返回 200 但缺少 `conduit_token` 导致任务失败。
+ [优化] 当旧的 `client_prepare_state=none` 不再返回 `conduit_token` 时自动 fallback 到 `success` 流程，并把 `missing conduit_token` 归类为可切号重试错误。
+ [诊断] prepare 阶段缺少 `conduit_token` 时记录返回 key/code/message，便于后续快速比对官方协议变化。

## 0.1.31 - 2026-07-19

+ [修复] 在线更新状态会识别固定 Docker 镜像 tag 部署，避免在 `IMAGE_POOL_IMAGE_TAG=0.x.x` 时误提示已启动升级；部署说明明确内置更新需使用 `latest` 模式。
+ [修复] 前端版本弹窗在固定 tag 部署时显示更新限制提示并禁用“立即升级”，避免用户误以为 Watchtower 已经切到新版本。

## 0.1.30 - 2026-07-19

+ [修复] `/v1/images/edits` 图生图接口兼容 OpenAI/new-api 常见参考图字段：`image[]`、`images[]`、`image[0]`、`images[0]`、`image_url`、`image_urls`、`referenceImages`、`reference_image`、`reference_images`、`input_image` 和 `input_images`。
+ [优化] 图生图 JSON 参考图解析支持字符串、数组、`url/image_url` 对象、`b64_json/base64`，减少“图片编辑缺少参考图”的误报。
+ [新增] 号池容量评估新增突发并发保障配置 `image_capacity_burst_parallel`，默认按 50 并发估算建议补号数量。

## 0.1.29 - 2026-07-19

+ [修复] 版本检测改为前端请求后端 `/api/system/latest-version` 代理接口，后端并发查询 GitHub Release、GitHub Tags 与 jsDelivr，避免浏览器直连发布源失败导致“获取最新版本信息失败”。
+ [优化] 发布源不可达时版本检测返回当前版本与 fallback 状态，不再弹出失败提示；Docker 运行镜像补充 `ca-certificates`，提升服务端 HTTPS 查询稳定性。

## 0.1.28 - 2026-07-19

+ [新增] 新增 `/api/image-pool/capacity` 动态号池容量评估接口，综合当前排队/运行任务、近 60 次任务耗时与成功率、账号冷却率、死号率和剩余额度，返回建议可用账号数与注册机建议补号数。
+ [新增] 账号池新增图片调度统计，返回可调度、空闲、占用、冷却、限流、失效、异常、死号率、冷却率和额度估算等容量评估指标。
+ [优化] 前端 API 封装新增 `fetchImagePoolCapacity` 与完整类型定义，方便后续注册机按接口结果动态补号。

## 0.1.27 - 2026-07-19

+ [调整] 生图与图生图提交到 ChatGPT Web 时恢复使用用户原始提示词，不再由后端自动融合人物一致性、参考图清洗、画幅或质量提示，具体约束交由用户自行填写。

## 0.1.26 - 2026-07-19

+ [修复] 首页“今日调用”、今日接口分布和今日模型使用改为按本地当天 00:00 起统计，不再混用历史总调用。
+ [修复] 首页“历史任务”改为统计 PostgreSQL 持久化历史总数，并补入尚未落库的内存新任务。
+ [优化] 图片管理默认加载真实低分辨率缩略图，缩略图会缓存到 `.thumbnails`，预览和下载仍使用原图。
+ [优化] 首页运行状况趋势从柱状图调整为成功/失败双曲线图，低并发下更易观察趋势。

## 0.1.25 - 2026-07-18

+ [新增] 图片接口支持 `output_format=png/jpeg/webp` 后端真实转码，`url` 与 `b64_json` 返回都会按请求格式输出。
+ [修复] ChatGPT Web 生图协议不再透传不支持的 `output_format` 元数据，避免格式参数影响上游生图语义。
+ [优化] 图片响应新增真实 `mime_type` 与 `format` 字段，前端展示 `b64_json` 时按实际 MIME 拼接 data URL。
+ [修复] 生成图下载优先使用 ChatGPT Web 当前 `/backend-api/files/download/{file_id}` 路径，并保留旧路径兜底。

## 0.1.24 - 2026-07-17

+ [修复] 任务队列页面改为读取 `/api/image-tasks/history` 持久化历史接口，完成任务从内存释放后仍可分页查看。
+ [修复] 任务实时状态接口支持从 PostgreSQL 持久化历史回查已释放的完成任务，避免历史列表打开详情时 404。

## 0.1.23 - 2026-07-17

+ [优化] 图生图提交到 ChatGPT Web 时增强编辑提示词权重，明确参考图仅作为素材并要求严格执行文字指令。
+ [优化] 将 `size` 与 `quality` 参数转换为画幅和质量提示写入 Web 生图 prompt，减少图生图忽略尺寸/清晰度要求的情况。

## 0.1.22 - 2026-07-16

+ [修复] 图生图结果解析同步过滤上传参考图的 `sediment://file_xxx` 引用，避免把输入参考图误判为生成结果导致几秒假成功。

## 0.1.21 - 2026-07-16

+ [新增] 新增 `/api/image-tasks/history` 任务历史分页接口，支持 `page`、`page_size` 与 `include_logs=1`。
+ [优化] PostgreSQL 任务历史改为持久保留；完成任务只从内存释放，不再删除数据库历史记录。
+ [优化] 服务启动时仅加载运行中/排队任务与最近完成任务，避免重启后历史任务全部进入内存。

## 0.1.20 - 2026-07-16

+ [修复] ChatGPT Web 生图提交后若长时间拿不到会话引用，会提前切换账号重试，避免任务无限拉长。
+ [修复] 图生图结果解析会过滤本次上传参考图，避免把输入图误判为生成结果。
+ [优化] 完成态任务自动压缩并清理：内存仅保留最近 30 分钟/最多 500 条完成任务，运行中任务不受影响。
+ [优化] PostgreSQL 同步删除过期完成任务，避免重启后重新加载大量历史任务推高内存。

## 0.1.19 - 2026-07-14

+ [修复] ChatGPT 生图轮询超时不再向 API 消费者暴露内部 `image poll timeout` 诊断，统一返回 HTTP 429 与“任务占用额度失败，请再次提交。”
+ [修复] 内容策略拦截类错误归为 rejected，不再计入失败率。
+ [新增] `/health/stability` 增加近 60 次请求成功率、失败率和平均生成耗时统计。

## 0.1.18 - 2026-07-14

+ [新增] 首页新增服务器负载卡片，展示 CPU、内存、网络上下行和图片存储磁盘使用情况。
+ [新增] 管理端提供 `/api/system/load` 实时负载接口，首页每 5 秒轻量刷新一次。
+ [调整] 首页移除“账号池状态”和“存储与代理”卡片；运行状况和错误率卡片等高对齐，错误原因列表限制在卡片内滚动。

## 0.1.17 - 2026-07-14

+ [优化] 生图任务采用有界并发调度、账号 FIFO 租约与批量状态持久化，提升高并发下的公平性与吞吐。
+ [修复] ChatGPT Web 生图准备、生成和跨账号重试使用固定 300 秒总预算；已提交会话仍可真实等待 180 秒。
+ [修复] OAuth 凭证失效会删除账号并重试；接口、SSE 与任务日志不再暴露上游 `token_revoked` 响应体。
+ [修复] 图片缓存下载、远程编辑图引用、PoW 与 Turnstile 回退增加超时、取消和并发保护。

## 0.1.16 - 2026-07-13

+ [优化] 生图调度优先使用刚导入账号池的账号；同一批新账号优先分配尚未跑过图的账号，避免重复压同一个账号。

## 0.1.15 - 2026-07-13

+ [调整] 单账号 ChatGPT 生图轮询上限从 60 秒延长至 180 秒，已有的 60 秒旧配置在升级后自动迁移为 180 秒。

## 0.1.14 - 2026-07-13

+ [修复] 最新版本检测同时查询 GitHub Release 与 jsDelivr 并选择较新版本，避免 jsDelivr 版本索引延迟导致前台收不到升级提示。

## 0.1.13 - 2026-07-13

+ [修复] 图片请求遇到 HTTP 401、`token_revoked` 等认证失败时立即删除失效账号，单个任务最多连续切换账号重试 10 次。

## 0.1.12 - 2026-07-13

+ [修复] 图片请求遇到明确的 401 `token_revoked` 时删除失效账号并切换账号重试，单个任务最多触发 1 次认证重试，即使错误发生在普通尝试上限的最后一次。
+ [修复] 取消默认 120 秒任务总截止时间；单个账号会话轮询超时后立即释放并切换账号，后续账号获得完整处理时间。
+ [优化] 无图片引用的 SSE 空流等待缩短为 8 秒且最多恢复 1 次，更早进入会话轮询。
+ [新增] 失败任务保留账号尝试次数、使用账号数和失败账号数，便于确认重试是否执行。

## 0.1.11 - 2026-07-13

+ [修复] 版本检查改用 jsDelivr 的最新 Release tag 解析接口，避免 GitHub 未认证 API 每小时 60 次的共享限流；GitHub API 仅作为失败兜底。
+ [修复] 更新日志按已发布版本读取固定 tag 内容，避免主分支内容与已完成镜像版本不一致。

## 0.1.10 - 2026-07-13

+ [修复] 普通生图不再重复执行账号 bootstrap、Sentinel 和额度预检，账号分配后立即进入实际生图；实际请求返回失效凭证时仍自动删除账号。
+ [修复] SSE 无图片结果时限制空闲等待和恢复次数，提前转入会话轮询；连接 `broken pipe` 和 `unexpected EOF` 会继续轮询，不再直接返回 502。
+ [优化] 任务进度改为按任务批量异步写入 PostgreSQL，自动迁移旧版全量任务文档，避免高并发下反复重写数 MB JSONB 阻塞请求。
+ [调整] 设置页移除已停用的生图账号预检间隔、并发和超时配置。

## 0.1.9 - 2026-07-13

+ [修复] 对齐当前 ChatGPT Web 生图协议：三阶段 prepare 串联 conduit token，复用 parent/trace，支持异步图片任务和 `/f/conversation/resume` 续传。
+ [修复] 按标准 SSE 事件读取图片结果；初始请求及续传遇到网络错误、408/409/425/429/502/504 时在同一账号内退避重试，避免无效换号和 300/600 秒等待。
+ [修复] 单会话轮询限制为 60 秒，图片请求默认总超时 120 秒；取消、内容策略拒绝和真实超时分别返回正确状态，不再统一记为 502。
+ [优化] 定时账号刷新默认调整为 60 分钟并使用轻量校验；导入和手动刷新仍执行完整验证并删除失效账号。
+ [优化] SSE 接口关闭 Nginx 响应缓冲，图片引用解析失败时保留具体上游诊断。
+ [新增] 任务队列表格和实时状态弹窗显示任务创建时间，提交时间与更新时间分开展示。

## 0.1.8 - 2026-07-13

+ [新增] 稳定性接口集成系统总览“运行状况”卡片的运行趋势、错误率和错误原因数据。

## 0.1.7 - 2026-07-12

+ [新增] 提供公开的 `/health/stability` 接口，持续返回最近 60 秒的调用稳定性与逐秒统计。

## 0.1.6 - 2026-07-12

+ [修复] 账号刷新间隔现在由后端持久化并按计划自动执行，保存设置后立即重置计时。

## 0.1.5 - 2026-07-12

+ [修复] 更新服务兼容 Docker API 1.40 及以上版本。

## 0.1.4 - 2026-07-12

+ [测试] 验证本地容器更新触发链路。

## 0.1.3 - 2026-07-12

+ [新增] GitHub Actions 镜像发布完成后自动创建对应版本的 GitHub Release。
+ [新增] 管理员可在控制台版本弹窗中一键拉取最新镜像并重启 IMAGE POOL 容器。
+ [调整] 版本检查以已发布 Release 为准，避免构建尚未完成时提前提示升级。

## 0.1.2 - 2026-07-12

+ [修复] 版本检查和仓库入口切换至 IMAGEPOOL 仓库。

## 0.1.1 - 2026-07-12

+ [发布] 更新 IMAGE POOL 服务版本。

+ [重构] 新增独立 Go 服务 IMAGE POOL，使用全新的数据目录，不迁移原项目数据。
+ [新增] ChatGPT Web 图片、文本 SSE 与搜索 mock 协议测试。
+ [新增] Go 注册执行链：TempMail.lol、Sentinel、PKCE、验证码校验、OAuth token 交换与 SSE 实时日志；全链路使用本地 mock 协议测试。
+ [新增] PostgreSQL 默认元数据存储：账号、用户 Key、调用日志、任务、图片标签和注册状态通过 JSONB 文档持久化；Compose 内置 PostgreSQL 健康检查。
+ [新增] 普通用户图片工作台和按 API Key 隔离的异步任务。
+ [移除] 不提供 PPT、PSD 或可编辑文件生成任务。

## 1.4.1 - 2026-06-03

+ [新增] 账号刷新改为异步模式，支持前端轮询刷新/重新登录进度。
+ [新增] 号池管理页面新增重新登录功能，支持密码登录恢复异常账号。
+ [新增] 刷新后自动重新登录异常账号（可在设置页开启）。
+ [新增] 图片生成支持并行模式，多张图片使用独立线程和账号同时生成。
+ [新增] 图片轮询超时自动换账号重试（最多4次），连接超时同账号递增等待重试。
+ [新增] 图片二次确认机制与先check再hit可配置化，关闭后可跳过等待直接返回结果。
+ [新增] 图片任务进度追踪，显示当前生成步骤（上传/预热/获取token/生成中等）。
+ [新增] 图片超时后续轮询功能，前端显示"继续等待"按钮。
+ [新增] 设置页新增图片二次确认、超时等待时间、自动重新登录等配置项。
+ [优化] 优化生图页面滚动加载性能，图片懒加载、会话切换滚动位置保存与恢复。

## 1.4.0 - 2026-05-31

+ [新增] 新增AI生成可编辑PSD文件逆向。
+ [新增] 新增AI生成可编辑PPT文件逆向。

## 1.3.1 - 2026-05-30

+ [新增] 新增ChatGPT搜索调试、Skills。

## 1.3.0 - 2026-05-30

+ [新增] 新增ChatGPT搜索接口逆向。

## 1.2.4 - 2026-05-30

+ [新增] 添加聊天补全缓存与重复请求合并。
+ [新增] 新增无限画布一键跳转功能

## 1.2.3 - 2026-05-29

+ [新增] 新增账号级代理。
+ [修复] 修复503异常信息、前端邮箱换行问题。

## 1.2.2 - 2026-05-29

+ [新增] 新增Codex链路生图、支持2k,4k。
+ [新增] 支持RT刷新账号信息。

## 1.2.0 - 2026-05-28

+ [新增] 当前版本基线，包含 Web 面板、画图、号池管理、注册机、图片管理和设置能力。
+ [新增] 前端版本号支持点击查看版本更新弹窗，展示当前版本、最新版本和更新日志。
+ [优化] 优化注册机效率，成功率大幅提高。
+ [优化] 优化生图页面配置选项。
