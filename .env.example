# Used only when a saved OpenAI refresh token is explicitly invalidated.
# The value must be supplied outside version control.
IMAGE_POOL_YYDS_MAIL_API_KEY=
IMAGE_POOL_YYDS_MAIL_API_BASE_URL=https://maliapi.215.im/v1
